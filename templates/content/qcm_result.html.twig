{% extends 'base.html.twig' %}

{% block title %}QCM Généré - {{ document.title }}{% endblock %}

{% block body %}
<div class="container py-5">
    <a href="{{ path('app_document_show', {'id': document.id}) }}" class="btn btn-outline-secondary mb-4 rounded-pill">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-left me-2" viewBox="0 0 16 16">
            <path fill-rule="evenodd" d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8z"/>
        </svg>
        Retour au document
    </a>

    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="d-flex align-items-center mb-4">
                <span class="fs-1 me-3">✨</span>
                <h1 class="fw-bold m-0">QCM Généré par l'IA</h1>
            </div>
            
            <p class="lead text-muted mb-5">
                Voici 5 questions basées sur le document <strong>{{ document.title }}</strong>.
            </p>

            {% for q in quiz %}
                <div class="card shadow-sm border-0 rounded-4 mb-4">
                    <div class="card-body p-4">
                        <h5 class="card-title fw-bold mb-3">{{ loop.index }}. {{ q.question }}</h5>
                        <ul class="list-group list-group-flush rounded-3 mb-3">
                            {% for option in q.options %}
                                <li class="list-group-item bg-light border-0 mb-1 rounded-3">
                                    {{ option }}
                                </li>
                            {% endfor %}
                        </ul>
                        
                        <div class="accordion" id="accordionAnswer{{ loop.index }}">
                            <div class="accordion-item border-0 bg-transparent">
                                <h2 class="accordion-header" id="heading{{ loop.index }}">
                                    <button class="accordion-button collapsed rounded-pill bg-success-subtle text-success shadow-none fw-bold px-4" type="button" data-bs-toggle="collapse" data-bs-target="#collapse{{ loop.index }}" aria-expanded="false" aria-controls="collapse{{ loop.index }}">
                                        👀 Voir la réponse
                                    </button>
                                </h2>
                                <div id="collapse{{ loop.index }}" class="accordion-collapse collapse" aria-labelledby="heading{{ loop.index }}" data-bs-parent="#accordionAnswer{{ loop.index }}">
                                    <div class="accordion-body text-success pt-2">
                                        <strong>Réponse(s) correcte(s) :</strong> 
                                        {% if q.answer is iterable %}
                                            {{ q.answer|join(', ') }}
                                        {% else %}
                                            {{ q.answer }}
                                        {% endif %}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            {% else %}
               <div class="alert alert-warning">
                   Aucune question n'a pu être générée. Vérifiez que le document contient du texte lisible.
               </div>
            {% endfor %}
            
            <div class="d-grid mt-5">
                 <a href="{{ path('app_document_generate_qcm', {'id': document.id}) }}" class="btn btn-primary btn-lg rounded-pill">
                    🔄 Régénérer un nouveau QCM
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
